{
  "name": "wristbandM2MClient-example",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "npm --prefix ../ install && npm --prefix ../ run build && npm install",
    "start": "tsc && node -r dotenv/config dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "ts-node": "^10.9.1",
    "typescript": "4.8.4"
  }
}
